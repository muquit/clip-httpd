sequenceDiagram
    actor User
    participant RemoteClient as Remote Client (pbcopy script)
    participant ClipSinkServer as ClipSink Server (Go App)
    participant OS as Target Machine OS

    User->>RemoteClient: Pipes text (e.g., echo "data" | pbcopy)
    Note over RemoteClient: Script wraps text in a curl command.

    RemoteClient->>ClipSinkServer: Sends HTTPS POST Request with data & API Key
    Note over RemoteClient, ClipSinkServer: Traffic is Encrypted (TLS) 🔒

    ClipSinkServer->>ClipSinkServer: 1. Authenticates API Key
    ClipSinkServer->>ClipSinkServer: 2. Reads text from request body
    ClipSinkServer->>OS: 3. Writes text to system clipboard
    OS-->>ClipSinkServer: Clipboard Updated

    ClipSinkServer-->>RemoteClient: Responds with 200 OK ✅
    RemoteClient-->>User: Command finishes